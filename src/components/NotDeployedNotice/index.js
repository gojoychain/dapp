import React from 'react';
import { Typography } from '@material-ui/core';

const NotDeployedNotice = () => (
  <Typography variant="h6" color="primary">
    Contract not deployed
  </Typography>
);

export default NotDeployedNotice;
