const styles = theme => ({
  root: {
    display: 'flex',
    flexDirection: 'row',
    paddingTop: theme.spacing.twoX,
    paddingBottom: theme.spacing.twoX,
  },
  title: {
    marginRight: theme.spacing.twoX,
  },
});

export default styles;
